import { defineComponent } from 'vue';
import { VFollower } from 'vueuc';

export default defineComponent({
  name: 'PopoverBody',
  inheritAttrs: false,
  setup() {},
  render() {
    return <VFollower></VFollower>;
  },
});
